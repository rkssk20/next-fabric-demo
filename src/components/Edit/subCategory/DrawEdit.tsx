const DrawEdit = ({ activeObject }: { activeObject: fabric.Object }) => {
  return (
    <div>
      <button>
        ペン
      </button>

      <button>
        消しゴム
      </button>
    </div>
  )
}

export default DrawEdit